%center Movie Streaming Server Read Me

%br/

%p
Michael B. Kulik<br/>
7/19/2011<br/>
Copyright 2011<br/>
%br/


I. Operating System Prerequisites<br/>
%table
	%tr
		%td
		%td 1. ruby
	%tr
		%td
		%td 2. ruby gems
	%tr
		%td
		%td 3. macruby + hotcocoa (for optional gui on OS X )


<br/>
II. Usage<br/>

%table
	%tr
		%td ./mvsctl -s, --start
		%td - starts the server on http://127.0.0.1:8080/
	%tr
		%td ./mvsctl -x, --stop
		%td - stops the server
	%tr
		%td ./mvsctl -d, --devel
		%td - starts the server in development mode on http://127.0.0.1:8080/
	%tr
		%td ./mvsctl -i, --install
		%td - installs the the required ruby gems 
	%tr
		%td ./mvsctl -b, --build
		%td - creates a distributable install for the movie server
	%tr
		%td ./mvsctl -c, --change PATH
		%td - changes the location of the movie directory to the specified path

%br/
%br/

III. GUI

%table
	%tr
		%td A standalone mac app with gui is provided as mvs.app, It depends on MacRuby and hotcocoa gem


